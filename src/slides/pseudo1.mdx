---
title: Pseudo code
type: content
content:
  content: |
    ```
    var randomValue = perlinNoise(); // A value between 0 - 1

    // each square
    rotate(randomValue * 360);
    ```
order: 60
---

