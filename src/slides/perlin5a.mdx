---
type: mdx
content:
  fullscreen: true
order: 55
---

import { CentreCols } from "../components/Common/CentreCols";
import { RandomGrid } from "../components/RandomGrid";

<CentreCols>
  <RandomGrid
    rows={200}
    cols={200}
    perlin
    perlinFactor={100}
    renderCell={(ctx, size, r) => {
      ctx.fillStyle = (r * 10) % 2 < 1.5 ? "black" : "white";
      ctx.fillRect(0, 0, size, size);
    }}
  />
</CentreCols>
