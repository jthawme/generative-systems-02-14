---
type: mdx
content:
  fullscreen: true
order: 57
---

import { CentreCols } from "../components/Common/CentreCols";
import { RandomGrid } from "../components/RandomGrid";

<CentreCols>
  <RandomGrid
    renderCell={(ctx, size, r) => {
      ctx.fillStyle = `rgb(${r * 255}, 0, 255)`;
      ctx.fillRect(0, 0, size, size);
    }}
  />
  <RandomGrid
    perlin
    perlinFactor={10}
    renderCell={(ctx, size, r) => {
      ctx.fillStyle = `rgb(${r * 255}, 0, 255)`;
      ctx.fillRect(0, 0, size, size);
    }}
  />
</CentreCols>
