---
title: Pseudo code
type: content
content:
  content: |
    ```
    // each square
    var randomValue = random(); // A value between 0 - 1
    rgb(randomValue * 255, randomValue * 255, randomValue * 255);
    ```
order: 46
---

